import { Card, CardBody } from '@heroui/react';

**How the starter project structure will look like:**

<Card className="mt-4">
  <CardBody className="bg-content2">
  <div className="max-w-full overflow-x-auto text-sm">
    <pre>
      ├── public
      ├── src                 # Core source code of the application
      │   ├── components      # Reusable UI elements 
      │   ├── config          # Configuration files of the application
      │   ├── hooks           # Custom React hooks for shared logic
      │   ├── layouts         # Page layouts or templates
      │   ├── pages           # Individual pages of the application
      │   ├── styles          # Global or component-specific styles
      │   ├── types           # TypeScript type definitions
      │   ├── App.tsx         # Root component of the application
      │   ├── main.tsx        # Entry point of the application
      │   ├── provider.tsx    # NextUI Provider component
      │   ├── vite-env.d.ts   # TypeScript declarations for Vite env variables
      ├── ...other files
      </pre>
  </div>
  </CardBody>
</Card>
